# Es lo mismo mismo mismo mismo que una lista
# solo tiene una sola condición: NO LA PUEDES MODIFICAR EN LO ABSOLUTO

numeros = (1, 2, 3) + (4, 5, 6)

# print(numeros)

# Transformar -> listas a tuple
punto = tuple([1,2, 3, 4, 5, 6, 7, 8])
print(punto)

menosNumeros = numeros[:3]
print(menosNumeros)